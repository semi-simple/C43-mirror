macOS:
  stage: build
  script:
    - make
  artifacts:
    paths:
      - wp43s
  tags:
    - macos

dm42:
  stage: build
  script:
    - cd DMCP_build
    - ./build_GMP_static_ARM_library
    - ./build_WP43S.pgm_for_DM42_hardware
  artifacts:
    paths:
      - DMCP_build/build/WP43S.pgm
  tags:
    - dm42

testSuite:
  stage: test
  script:
    - make testSuite
  tags:
    - macos
    - linux
    - windows
