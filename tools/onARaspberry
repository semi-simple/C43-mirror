CPUINFO=/proc/cpuinfo

if [ -f $CPUINFO ]; then
	grep -q Raspberry $CPUINFO
	if [ $? = 0 ]; then
		echo "YES$(tools/32or64bit.c)"
	else
		echo "NO$(tools/32or64bit.c)"
	fi
else
	echo "NO$(tools/32or64bit.c)"
fi
